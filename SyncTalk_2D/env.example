# SyncTalk FastAPI Configuration
# Copy this file to .env and update the values as needed

# Remote avatar configuration URL (optional)
# If provided, the server will fetch avatar configs from this URL
# Falls back to local file if remote fetch fails
AVATAR_CONFIG_REMOTE_URL=https://your-cdn.example.com/avatars/config.json

# Local avatar configuration file path (fallback)
# Used when remote URL is not provided or fails to load
# Falls back to built-in defaults if this file doesn't exist
AVATAR_CONFIG_LOCAL_PATH=./avatar_config.json

# Example with S3/CloudFront URL:
# AVATAR_CONFIG_REMOTE_URL=https://d123456789.cloudfront.net/synctalk/avatars.json

# Example with GitHub raw URL:
# AVATAR_CONFIG_REMOTE_URL=https://raw.githubusercontent.com/yourusername/synctalk-configs/main/avatars.json

# Example with custom API endpoint:
# AVATAR_CONFIG_REMOTE_URL=https://api.yourcompany.com/v1/synctalk/avatars
